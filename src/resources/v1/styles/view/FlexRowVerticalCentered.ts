import {ViewStyle} from 'react-native';
import FlexRow from './FlexRow';

const FlexRowVerticalCentered: ViewStyle = {
  ...FlexRow,
  alignItems: 'center',
  flex: 1,
};

export default FlexRowVerticalCentered;
